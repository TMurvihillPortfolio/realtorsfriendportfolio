const styles = theme => ({
    root: {
        marginTop: '50px',
        width: '100%',
        marginTop: theme.spacing(3),
        overflowX: 'auto',
        paddingTop: '25px',
    },
    table: {
    minWidth: 650,
    }
});
export default styles;